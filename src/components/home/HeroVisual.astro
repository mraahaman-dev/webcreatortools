<div class="relative mx-auto h-[420px] w-full max-w-lg">
  <!-- Floating gradient blobs behind the showcase card -->
  <div class="pointer-events-none absolute -left-10 -top-10 h-64 w-64 rounded-full bg-brand-300/30 blur-3xl animate-blob-a"></div>
  <div class="pointer-events-none absolute -bottom-10 -right-6 h-64 w-64 rounded-full bg-purple-300/30 blur-3xl animate-blob-b"></div>

  <!-- Showcase card -->
  <div class="relative h-full w-full overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-md">
    <div class="flex items-center gap-1.5 border-b border-slate-100 bg-slate-50 px-4 py-3">
      <span class="h-2.5 w-2.5 rounded-full bg-red-300"></span>
      <span class="h-2.5 w-2.5 rounded-full bg-amber-300"></span>
      <span class="h-2.5 w-2.5 rounded-full bg-green-300"></span>
      <span class="ml-2 text-xs font-medium text-slate-400">pixelquil.com</span>
    </div>

    <div class="relative h-[calc(100%-45px)] w-full">
      <!-- Slide 1: SVG Palette Extractor -->
      <div class="hero-slide absolute inset-0 p-6" style="animation-delay:0s;">
        <div class="mb-3 text-xs font-semibold uppercase tracking-wide text-amber-600">SVG Palette Extractor</div>
        <div class="mb-4 flex h-24 items-center justify-center rounded-xl border border-slate-100 bg-slate-50">
          <svg class="h-12 w-12 text-amber-500" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 2a10 10 0 100 20 2 2 0 001-3.73A2 2 0 0114 16a2 2 0 012-2h1.5a3.5 3.5 0 003.5-3.5C21 6 17 2 12 2z"/></svg>
        </div>
        <div class="flex gap-2">
          <span class="h-8 w-8 rounded-lg bg-amber-400"></span>
          <span class="h-8 w-8 rounded-lg bg-orange-400"></span>
          <span class="h-8 w-8 rounded-lg bg-pink-400"></span>
          <span class="h-8 w-8 rounded-lg bg-indigo-400"></span>
          <span class="h-8 w-8 rounded-lg bg-slate-200"></span>
        </div>
      </div>

      <!-- Slide 2: Color Format Converter -->
      <div class="hero-slide absolute inset-0 p-6" style="animation-delay:3s;">
        <div class="mb-3 text-xs font-semibold uppercase tracking-wide text-blue-600">Color Format Converter</div>
        <div class="mb-3 h-14 rounded-xl bg-gradient-to-r from-indigo-500 to-blue-500"></div>
        <div class="space-y-2">
          <div class="flex items-center justify-between rounded-lg bg-slate-50 px-3 py-2 text-xs"><span class="text-slate-400">HEX</span><span class="font-mono text-slate-700">#4F46E5</span></div>
          <div class="flex items-center justify-between rounded-lg bg-slate-50 px-3 py-2 text-xs"><span class="text-slate-400">OKLCH</span><span class="font-mono text-slate-700">54% 0.25 275</span></div>
        </div>
      </div>

      <!-- Slide 3: UUID Generator -->
      <div class="hero-slide absolute inset-0 p-6" style="animation-delay:6s;">
        <div class="mb-3 text-xs font-semibold uppercase tracking-wide text-teal-600">UUID Generator</div>
        <div class="space-y-1.5 rounded-xl border border-slate-100 bg-slate-50 p-3 font-mono text-[10px] text-slate-500">
          <div>a1b2c3d4-e5f6-4a7b-8c9d-0e1f2a3b4c5d</div>
          <div>f6e5d4c3-b2a1-4f6e-9d8c-7b6a5f4e3d2c</div>
          <div>3c2b1a0f-9e8d-4c3b-2a1f-0e9d8c7b6a5f</div>
        </div>
        <div class="mt-3 inline-block rounded-lg bg-teal-600 px-3 py-1.5 text-xs font-medium text-white">Generate</div>
      </div>

      <!-- Slide 4: QR Code Generator -->
      <div class="hero-slide absolute inset-0 p-6" style="animation-delay:9s;">
        <div class="mb-3 text-xs font-semibold uppercase tracking-wide text-green-600">QR Code Generator</div>
        <div class="flex justify-center">
          <div class="grid h-28 w-28 grid-cols-6 grid-rows-6 gap-0.5 rounded-lg border border-slate-100 bg-white p-2">
            <!-- Decorative pseudo-QR pattern, not a real scannable code -->
            <span class="col-span-2 row-span-2 rounded-sm bg-slate-800"></span><span></span><span class="rounded-sm bg-slate-800"></span><span></span><span class="rounded-sm bg-slate-800"></span>
            <span></span><span></span><span class="rounded-sm bg-slate-800"></span><span></span><span class="rounded-sm bg-slate-800"></span><span></span>
            <span class="rounded-sm bg-slate-800"></span><span class="rounded-sm bg-slate-800"></span><span></span><span class="col-span-2 row-span-2 rounded-sm bg-slate-800"></span><span></span>
            <span></span><span class="rounded-sm bg-slate-800"></span><span></span><span></span><span></span><span class="rounded-sm bg-slate-800"></span>
            <span class="col-span-2 row-span-2 rounded-sm bg-slate-800"></span><span></span><span class="rounded-sm bg-slate-800"></span><span></span><span class="rounded-sm bg-slate-800"></span>
            <span></span><span></span><span class="rounded-sm bg-slate-800"></span><span></span><span class="rounded-sm bg-slate-800"></span><span></span>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  @keyframes blobFloatA {
    0%, 100% { transform: translate(0, 0) scale(1); }
    50% { transform: translate(24px, -28px) scale(1.08); }
  }
  @keyframes blobFloatB {
    0%, 100% { transform: translate(0, 0) scale(1); }
    50% { transform: translate(-20px, 22px) scale(0.94); }
  }
  .animate-blob-a { animation: blobFloatA 9s ease-in-out infinite; }
  .animate-blob-b { animation: blobFloatB 11s ease-in-out infinite; }

  @keyframes heroSlideFade {
    0%, 2% { opacity: 0; transform: translateY(6px); }
    6%, 23% { opacity: 1; transform: translateY(0); }
    27%, 100% { opacity: 0; transform: translateY(-6px); }
  }
  .hero-slide {
    opacity: 0;
    animation: heroSlideFade 12s ease-in-out infinite;
  }

  @media (prefers-reduced-motion: reduce) {
    .animate-blob-a, .animate-blob-b, .hero-slide {
      animation: none;
    }
    .hero-slide:first-child { opacity: 1; }
  }
</style>